# Local temporary downloads and IDE settings
*.downloading
.ipynb_checkpoints/
.idea/
.vscode/

# Keep the source experiment layout and generated artifacts in Git.
# Large .pth and .tif files are stored through Git LFS via .gitattributes.
